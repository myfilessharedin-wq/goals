pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/
    - cp app.js public/
    - cp firebase.js public/
    - cp style.css public/
  artifacts:
    paths:
      - public
  only:
    - main
